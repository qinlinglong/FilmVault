# 构建说明（影库 FilmVault · 原生 Android App）

## 为什么没有一个可直接下载安装的 APK？

这个 App 是用 **Kotlin + Jetpack Compose** 写的**真正原生应用**（不是 WebView 套壳）。
APK 必须用 Android SDK 编译出来，而本项目的生成环境存在硬性限制：

- 沙箱里**没有安装 Android SDK**；
- Google 的 SDK 下载源（`dl.google.com`）在当前网络环境下被阻断；
- 国内能下二进制的是腾讯云镜像，但它只镜像了 Gradle，**不镜像 Android SDK**；
- USTC / SJTU 的 Android SDK 镜像要么刷完验证返回 404，要么是浏览器 PoW 挑战（无头浏览器解不出）；
- GitHub 也连不上。

因此在这里**无法编译出 APK**。但**全部源码已写完并自审过**，你只要在本机（有 Android SDK + 能联网）按下面步骤，2 分钟就能出包。

> 工程的 Gradle 仓库已经配好**阿里云镜像**（`settings.gradle.kts`），在国内同步依赖不会卡。

---

## 前置条件

- **JDK 17+**（推荐用 Android Studio 自带的）
- **Android SDK**（API 34 平台 + build-tools 34.0.0），Android Studio 会帮你装
- 能访问 Maven（国内已走阿里云镜像，无需翻墙）

---

## 方式一：Android Studio（最省事，推荐）

1. 把整个 `/workspace` 目录拷到电脑（目录名随意，例如 `FilmVault`）。
2. 用 **Android Studio（Hedgehog / Iguana 及以上）** 打开该目录。
3. 等待 Gradle 同步完成（首次会下载 AGP / Compose / Kotlin 等依赖，已走阿里云镜像）。
4. 顶部菜单 **Build → Build Bundle(s) / APK(s) → Build APK(s)**。
5. 编译完成后，右下角会弹通知，点 **locate** 打开产物：
   - 调试包：`app/build/outputs/apk/debug/app-debug.apk`
   - 正式签名包：`app/build/outputs/apk/release/app-release.apk`

把 `app-debug.apk` 或 `app-release.apk` 拖到手机（或 `adb install`）即可安装。

---

## 方式二：命令行（Gradle）

```bash
# 1. 进入工程目录
cd FilmVault

# 2. 配置 SDK 路径（Android Studio 会自动生成；纯命令行需要手动指定）
#    在 local.properties 里加一行（Windows 用反斜杠）：
#    sdk.dir=/Users/你/Library/Android/sdk
#    Linux 示例：
#    sdk.dir=/home/你/Android/Sdk

# 3. 编译调试包（自动用调试密钥签名，可直接安装）
./gradlew assembleDebug

# 或编译正式签名包（请使用本地私有签名配置）
./gradlew assembleRelease
```

产物位置同上。

> Windows 上把 `./gradlew` 换成 `gradlew.bat`。

---

## 关于签名

- **调试包**：Android Gradle Plugin 自动用调试密钥签名，开「USB 调试」即可安装。
- **正式包**：正式签名密钥不得提交到仓库；请使用本地签名配置，并通过环境变量或 CI Secret 注入密码。

---

## 登录与账号

App 首次进入是登录页，用户需要填写自己的模块化仓库地址、账号和密码；项目不提供测试账号，也不支持游客绕过登录。

> 注：在线播放 / 网盘 / 磁力 三类资源走的是系统原生 Intent 调起（调用本机播放器、网盘 App、磁力工具），
> 这是 Android 原生 App 的标准做法，不是 WebView 套壳。

---

## 目录结构速览

```
app/src/main/java/com/filmvault/app/
├── MyApp.kt / MainActivity.kt        # 入口
├── util/                             # 常量、PoW 验证、Cookie、收藏、播放 Intent
├── data/remote/ApiClient.kt          # 原生网络层（PoW 握手 / 列表 / 详情 / 资源）
├── data/model/Models.kt
├── data/repository/FilmRepository.kt
├── di/AppModule.kt
├── viewmodel/FilmViewModels.kt
├── ui/theme/Theme.kt
├── ui/components/MovieCard.kt
├── ui/screens/                      # Login / Home / Catalog / Detail / Search / Library / Player
└── ui/navigation/NavGraph.kt
```

更完整的接口逆向说明见 `README.md`。
